#!/usr/bin/env bash

# Terminate already running bar instances
killall -q polybar

# Wait until the processes have been shut down
while pgrep -x polybar -u $UID >/dev/null; do sleep 1; done

MONITOR=DP-1 polybar --reload topbar &
